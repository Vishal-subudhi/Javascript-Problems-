// JavaScript Program to Check Whether a String Starts and Ends With Certain Characters

let char1 = "a";

let str = "ama";

if (str.startsWith(char1) || str.endsWith(char1)) {
  console.log(`it starts and end with ${char1}`);
} else console.log(`it does not start and end with ${char1}`);
